{
  "entities": {
    "userProfile": {
      "title": "User Profile",
      "description": "Represents a user's profile information.",
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "photoURL": {
          "type": "string",
          "format": "uri",
          "description": "URL of the user's profile picture."
        }
      },
      "required": ["displayName", "email"]
    },
    "exerciseResult": {
      "title": "Exercise Result",
      "description": "Stores the result of a user completing an exercise set.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The ID of the user who completed the exercise."
        },
        "exerciseSlug": {
          "type": "string",
          "description": "The slug of the completed exercise set."
        },
        "exerciseTitle": {
          "type": "string",
          "description": "The title of the completed exercise set."
        },
        "score": {
          "type": "number",
          "description": "The number of correctly answered questions."
        },
        "totalQuestions": {
          "type": "number",
          "description": "The total number of questions in the exercise."
        },
        "percentage": {
          "type": "number",
          "description": "The user's score as a percentage."
        },
        "completedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the exercise was completed."
        }
      },
      "required": ["userId", "exerciseSlug", "exerciseTitle", "score", "totalQuestions", "percentage", "completedAt"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/userProfile"
      },
      "description": "Stores public profile information for users."
    },
    "/users/{userId}/exerciseResults/{resultId}": {
      "schema": {
        "$ref": "#/entities/exerciseResult"
      },
      "description": "Stores the results of exercises completed by a user."
    }
  }
}
